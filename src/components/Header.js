import React from 'react'

const Header = ({titulo}) => {
    return (
        <div>
            <header>

            <h1>{titulo}</h1>
            </header>
        </div>
    )
}

export default Header
